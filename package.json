{
  "name": "anne-marketing-website",
  "version": "1.0.0",
  "description": "ANNE - Personal AI Assistant for Older Adults",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
